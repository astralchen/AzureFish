# 2026-09-24 聊天代码增量同步

后续已完成两项动画测试时序修复及日志启动参数开关，见 [修复与复验记录](animation-diagnostics-2026-09-24.md)。以下保留增量同步当时的结果。

本次从 QuickLayoutKit 的 `84c64cb44be82250df0114f4f28ae50a2fc720c9` 同步到 `54d74d9032e85f310f122493256f41bc04790a41`，保留 AzureFish 的应用入口、iOS 15 部署目标和远程框架引用。

## 同步内容

- 列表安全区、导航栏顶部遮挡变化时保持阅读位置，避免纵向时间线产生横向滚动范围。
- 文字气泡重命名为 `TextBubbleCell`、`TextBubbleView`；文字、语音和文件卡片改用 `ContainerRelativeSize`，移除旧 `BubbleWidth`。
- Live Photo 导入通过声明的 UTI 分流，使用明确类型的加载接口，保留取消后丢弃迟到结果的行为。
- 同步相关单元、组件和 UI 测试。当前 121 个聊天 Swift 文件与来源逐一比对一致，只有 AzureFish 日志命名替换。
- QuickLayoutKit 仍为远程 `main` 引用，本次锁文件更新到 `54d74d9032e85f310f122493256f41bc04790a41`。其余包的解析提交未变。
- 37 个媒体资源文件与来源一致，本轮无需替换。
- 将 `imessage.legacy.pending` 和 `CFBundleDisplayName` 的过期提取状态改为手动管理，保留全部翻译。

AzureFish 专用测试适配：继续保留模块名、UI 测试导航参数和 iOS 15 等待 API 的转换；给新增 Live Photo 导入测试补充可用性保护；将菜单截图像素统计改为等价显式循环，解决 Xcode 26.6 的类型推断超时。未修改业务逻辑来规避测试断言。

## 验证

环境：Xcode 26.6（17F113）、iPhone 17 Pro / iOS 26.5 模拟器。

| 项目 | 结果 |
| --- | --- |
| App、单元测试和 UI 测试目标构建 | 通过，iOS 15.0 部署配置 |
| 聊天源码与媒体资源逐一比对 | 通过：121 个 Swift 文件、37 个资源文件 |
| 本地化目录及编译输出 | 通过：144 个 Localizable 条目、6 个 InfoPlist 条目，三种语言完整且无 stale 状态 |
| 首轮选定单元/组件回归 | 84 项中 82 项通过、2 项动画断言失败 |
| 单独复验 DemoTests | 32 项全部通过，包含首轮失败的两项 |
| 正常启动、输入和发送 UI | 通过 |
| 左右拖动后消息横向位置 UI | 通过 |
| 返回底部后连续下拉进入历史 UI | 通过 |
| 发出文字菜单连续打开/收起 8 次 UI | 通过 |
| 正常入口截图检查 | 通过，见 `sync-2026-09-24.png` |
| 真机、iOS 15～25 运行、全矩阵与性能 A/B | 未执行 |

首轮失败为 `composerDictationAndInterruptedHintShareContinuousGeometry` 和 `composerAudioPanelAnimatesWithoutRestartingForMeterUpdates`，分别在固定延时后的中间动画高度、退出视图清理断言处失败。单独复跑套件未复现，保留为动画测试时序稳定性关注项；首轮结果不能表述为全绿。

## 验证证据

- 构建：`/tmp/azurefish-sync-build-final.log`
- 首轮测试：`/tmp/azurefish-sync-tests.log`、`/tmp/AzureFish-Sync-20260924.xcresult`
- 动画复验：`/tmp/azurefish-sync-animation-recheck.log`、`/tmp/AzureFish-Sync-Animation-Recheck.xcresult`
- 本轮变更前文件备份：`/tmp/azurefish-chat-sync-20260924/before`

同步未修改 QuickLayoutKit 源工程，未提交 Git。iOS 15～25 完整聊天 UI 仍待后续适配。
